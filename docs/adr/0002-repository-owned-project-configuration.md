# 使用仓库内的 TOML 项目配置

每个开发项目使用仓库根目录的 `.devcontrol.toml` 作为可共享、可版本控制的项目配置，应用本地存储仅保留个人状态。配置包含 `schema_version`，路径相对于开发项目根目录，并以共享默认值配合 `macos`、`windows` 局部覆盖表达平台差异；配置文件可以引用外部 `.env` 文件并保存非敏感环境值，但不得复制、展示或记录密钥，以兼顾跨平台复现与仓库安全。
