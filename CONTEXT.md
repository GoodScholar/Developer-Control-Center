# Developer Control Center

Developer Control Center 是面向个人全栈开发者的开源本地开发服务管理产品。它将一个开发项目中的多个长期运行服务作为整体管理，让开发者无需依赖多个分散的终端窗口掌握项目运行状态。

## Language

**开发项目（Development Project）**：
开发者选择并交由产品管理的本地工作目录；它是组织开发服务和运行状态的边界。
_Avoid_：工程、仓库、工作区

**开发服务（Development Service）**：
隶属于某个开发项目、需要持续运行直至被停止或异常退出的本地进程。
_Avoid_：任务、脚本、作业

**项目运行（Project Run）**：
一个开发项目中一组开发服务从启动到全部停止的完整运行周期。
_Avoid_：会话、环境、实例

**项目配置（Project Configuration）**：
开发者确认并保存的开发项目定义，声明该项目包含哪些开发服务以及它们期望的运行方式。
_Avoid_：工程配置、工作区配置、启动脚本

**检测建议（Detection Proposal）**：
产品根据开发项目中的现有文件推断出的候选项目配置；在开发者确认前，它不具备执行权限。
_Avoid_：自动配置、扫描结果、默认配置

**服务依赖（Service Dependency）**：
一个开发服务在进入可用状态前必须先可用的另一个开发服务。
_Avoid_：启动顺序、前置脚本

**可用状态（Ready State）**：
开发服务已经达到允许依赖它的下游服务启动的状态；没有健康检查时对应运行中，配置健康检查时对应健康。
_Avoid_：已启动、已完成、在线

**受管进程（Managed Process）**：
产品在某次项目运行中为开发服务启动并持续追踪的进程及其子进程树。
_Avoid_：本地进程、后台任务

**外部进程（External Process）**：
不属于当前项目运行的进程，即使它占用了开发服务需要的端口，也不能被产品自动终止。
_Avoid_：冲突进程、未知进程

**健康检查（Health Check）**：
针对运行中开发服务的 HTTP 或 TCP 探测，用于判断它是否已经可供其他服务或开发者使用。
_Avoid_：存活检查、状态检查、监控

**服务失败（Service Failure）**：
开发服务在未收到停止请求时退出，或始终无法达到配置要求的可用状态。
_Avoid_：崩溃、停止、离线

**运行日志（Run Log）**：
开发服务在一次项目运行期间产生并由产品采集的标准输出与标准错误记录。
_Avoid_：终端历史、控制台、审计日志

**优雅停止（Graceful Stop）**：
产品先请求受管进程自行结束并等待限定时间，再对仍未退出的完整进程树执行强制终止。
_Avoid_：关闭、杀进程、退出

**预期端口（Expected Port）**：
项目配置为开发服务声明的可选监听端口，用于在启动前发现冲突并在运行后定位服务入口。
_Avoid_：服务端口、扫描端口、占用端口

**待处理状态（Needs Attention）**：
产品发现无法安全自动处置的运行情况，需要开发者明确选择下一步，例如应用异常退出后遗留的外部进程。
_Avoid_：错误、警告、失败

**阻塞服务（Blocked Service）**：
因为至少一个服务依赖未达到可用状态而尚未启动的开发服务；阻塞本身不表示该服务启动失败。
_Avoid_：失败服务、停止服务、等待服务

**降级运行（Degraded Run）**：
项目运行仍有开发服务可用，但至少一个目标服务失败、不健康或被阻塞的状态。
_Avoid_：部分成功、异常运行、失败项目

**项目注册（Project Registration）**：
开发者把一个开发项目加入产品的个人项目列表；取消注册只移除产品保存的个人记录，不改变开发项目本身。
_Avoid_：导入、安装、创建项目

**本地元数据（Local Metadata）**：
产品仅为当前开发者保存的项目注册信息、界面偏好和运行摘要，不属于可共享的项目配置。
_Avoid_：项目数据、用户配置、缓存

**诊断包（Diagnostic Bundle）**：
开发者主动导出的故障排查材料集合；其中不得包含环境变量值或其他产品能够识别的密钥内容。
_Avoid_：遥测、崩溃上传、日志备份

**合并日志（Merged Log）**：
按照采集时间汇总一次项目运行中所有开发服务输出的统一视图，每条记录仍保留其来源服务。
_Avoid_：控制台、全局日志、日志文件

**本地运行（Local Operation）**：
项目配置、本地元数据、运行日志和进程操作均留在开发者设备上的产品边界；除用户配置的健康检查与版本检查外，不依赖远程服务。
_Avoid_：离线模式、本地账户、私有部署
