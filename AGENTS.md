## Agent skills

### Issue tracker

项目使用 `.scratch/` 下的本地 Markdown 文件跟踪工作。详见 `docs/agents/issue-tracker.md`。

### Triage labels

项目采用五个默认 Triage 状态。详见 `docs/agents/triage-labels.md`。

### Domain docs

项目采用单上下文领域文档布局。详见 `docs/agents/domain.md`。
